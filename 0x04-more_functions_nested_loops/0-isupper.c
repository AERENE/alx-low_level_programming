#include "main.h"
/**
 * _isupper - Returns 1 if c is uppercase
 * @c: compares the digits
 * Return: Always 0.
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}

}

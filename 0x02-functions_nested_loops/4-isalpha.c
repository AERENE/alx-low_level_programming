#include "main.h"
/**
 * int _isalpha(int c) - Returns 1 if c is a letter, lowercase or uppercase. Returns 0 otherwise.
 *
 * Return: Always 0.
 */
int _isalpha(int c)

{

if (c > 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}

}
